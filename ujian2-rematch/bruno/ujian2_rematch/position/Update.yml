info:
  name: Update
  type: http
  seq: 1

http:
  method: PUT
  url: "{{baseURL}}/positions/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
          "title": "Software Enginner",
          "base_salary": 8000000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
