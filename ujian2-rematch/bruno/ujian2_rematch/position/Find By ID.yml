info:
  name: Find By ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/positions/:id"
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
