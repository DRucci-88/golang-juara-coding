info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/positions"
  body:
    type: json
    data: |-
      {
          "title": "Software Enginner",
          "base_salary": 7000000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
