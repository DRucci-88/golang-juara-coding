info:
  name: Auth JWT
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/auth/jwt"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
