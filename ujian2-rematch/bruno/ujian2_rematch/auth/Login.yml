info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "admin@company.co.id",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
