info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/departments"
  body:
    type: json
    data: |-
      {
          "name": "HR",
          "code": "DEPT-HR"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
