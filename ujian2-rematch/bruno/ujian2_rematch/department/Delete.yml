info:
  name: Delete
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseURL}}/departments/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
