info:
  name: Update
  type: http
  seq: 1

http:
  method: PUT
  url: "{{baseURL}}/departments/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
          "name": "HR",
          "code": "DEPT-HR"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
