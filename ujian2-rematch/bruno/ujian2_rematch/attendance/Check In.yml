info:
  name: Check In
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/attendances/check-in"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
