info:
  name: Check Out
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/attendances/check-out"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
